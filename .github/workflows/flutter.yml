name: Build APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.24.0
          cache: true

      - name: Flutter Version
        run: flutter --version

      - name: Generate Android Scaffold (if missing)
        run: |
          if [ ! -f "android/app/build.gradle" ] && [ ! -f "android/app/build.gradle.kts" ]; then
            if [ -f "android/app/src/main/AndroidManifest.xml" ]; then
              cp android/app/src/main/AndroidManifest.xml /tmp/custom-AndroidManifest.xml
            fi

            flutter create --platforms=android --org com.example --project-name whatsapp_media_cleaner _android_scaffold

            rm -rf android
            mv _android_scaffold/android ./android
            rm -rf _android_scaffold

            if [ -f "/tmp/custom-AndroidManifest.xml" ]; then
              cp /tmp/custom-AndroidManifest.xml android/app/src/main/AndroidManifest.xml
            fi
          fi

      - name: Install Dependencies
        run: flutter pub get

      - name: Build Release APK
        run: flutter build apk --release

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: whatsapp-media-cleaner-release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error
